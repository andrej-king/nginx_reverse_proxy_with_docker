services:
  nginx:
    build:
      context: ./docker
      dockerfile: nginx/Dockerfile
    depends_on:
      - node_service_2
    networks:
      - default_network
    ports:
      - '8081:80'

  node:
    build:
      context: ./docker
      dockerfile: node/Dockerfile
    command: sh -c 'npm install && npm start'
    networks:
      - default_network
    volumes:
      - ./app:/app

networks:
  default_network:
    external: false
